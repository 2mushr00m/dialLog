<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:title="@string/btn_add_bookmark"
        android:id="@+id/action_add_bookmark"
        android:icon="@drawable/ic_add_bookmark"
        app:showAsAction="never" />

    <item
        android:title="@string/btn_edit_name"
        android:id="@+id/action_edit_name"
        android:icon="@drawable/ic_edit"
        app:showAsAction="never" />

    <item
        android:title="@string/btn_delete_cache"
        android:id="@+id/action_delete_cache"
        android:icon="@drawable/ic_delete_cache"
        app:showAsAction="never" />

</menu>